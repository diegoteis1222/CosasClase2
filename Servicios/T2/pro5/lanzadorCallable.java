
import java.util.concurrent.Callable;

public class lanzadorCallable implements Callable<Integer> {

    public Integer call() throws Exception {
        int cuenta = 0;
        char objetico = vocal.toLowerCase().charAt(0);

        try()
    }
}
